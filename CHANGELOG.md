# Changelog

## v0.3.51

- Adds a repeatable Puppeteer screenshot capture script.
- Captures popup plus every Chrome options tab for README documentation.
- Adds the extension screenshot command to `extension/package.json`.
- Wraps the Setup tab install command so it screenshots cleanly.

## v0.3.50

- Adds an About TubeVault block to the options Support tab.
- Documents the local-first privacy model in the extension UI.
- Adds Puppeteer-captured options and popup screenshots.

## v0.3.49

- Fixes the extension build workflow for the standalone TubeVault repository.
- Adds GitHub-ready README, setup, changelog, support, privacy, and local path documentation.
- Keeps the Windows unpacked extension copy in sync with the current build.

## v0.3.48

- Current extracted TubeVault repo version.
- Adds local ops metadata tracking.
- Keeps TubeVault as its own GitHub repo at `n8watkins/tube-vault`.

## v0.3.47

- Adds generated Chrome extension icons and manifest icon wiring.
- Improves archive button extension assets.

## v0.3.46

- Updates extension build output after playlist and mix support changes.

## v0.3.45

- Updates extension build output after popup/options/content-script improvements.

## v0.3.44

- Updates options and service worker build output.

## v0.3.43

- Updates options and service worker build output after exact-folder capture work.

## Earlier v0.3 Releases

- Adds playlist and YouTube Mix support.
- Adds per-video download jobs, batch grouping, queue visibility, and batch selection planning.
- Adds component-aware size estimates and batch summary output.
- Adds configurable save layout, folder-per-video mode, title filenames, per-video summaries, and exact-folder capture.
- Adds helper probe/list video actions and stronger native-helper behavior.
- Adds options sections for history, setup/status, and support planning.
